<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/band_fragment">

    <fragment
        android:id="@+id/band_fragment"
        android:name="com.example.rockit.pages.band.BandPageFragment"
        android:label="@string/band_fragment_name"
        tools:layout="@layout/band_fragment">
        <!--<action
            android:id="@+id/action_sleepTrackerFragment_to_sleepQualityFragment"
            app:destination="@id/sleep_quality_fragment"
            app:enterAnim="@anim/slide_in_right"
            app:popEnterAnim="@anim/slide_in_right" />-->
    </fragment>

</navigation>